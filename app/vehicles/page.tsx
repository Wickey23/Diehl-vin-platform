import Link from "next/link";
import { AppShell } from "../../components/app-shell";
import { sampleVehicles } from "../../lib/vehicle-data";

export default function VehiclesPage(){return <AppShell><div className="page-title"><p className="eyebrow">FLEET DATABASE</p><h1>Vehicles</h1><p>VIN master records enriched with DTNA order and in-service information.</p></div><div className="toolbar"><input placeholder="Search VIN, serial, customer or model"/><select defaultValue="all"><option value="all">All statuses</option><option>Scheduled</option><option>In Process</option><option>Dealer Received</option></select><Link className="primary" href="/lookup">New lookup</Link></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>VIN</th><th>Serial</th><th>Model</th><th>Customer</th><th>Status</th><th>Status Date</th><th>Projected Delivery</th></tr></thead><tbody>{sampleVehicles.map(v=><tr key={v.vin}><td><Link className="mono" href={`/vehicles/${v.vin}`}>{v.vin}</Link></td><td>{v.serial}</td><td>{v.model}</td><td>{v.customer}</td><td><span className="status">{v.status}</span></td><td>{v.statusDate}</td><td>{v.projectedDelivery||"—"}</td></tr>)}</tbody></table></div><p className="demo-note">Demo records are masked/generalized. Connect Supabase and import your workbook to populate production data.</p></section></AppShell>}
