import { AppShell } from "../../components/app-shell";
export default function AdminPage(){return <AppShell><div className="page-title"><p className="eyebrow">ADMINISTRATION</p><h1>Settings</h1><p>Configure database, worker and data behavior without exposing credentials.</p></div><section className="panel"><h2>Integration status</h2><div className="detail-grid"><div><span>Supabase</span><b>Environment variables required</b></div><div><span>DTNA Worker</span><b>Environment variables required</b></div><div><span>Automatic Git deployments</span><b>Disabled by vercel.json</b></div></div></section><section className="panel"><h2>Dynamic fields</h2><p>DTNA and template fields that are not part of the core schema are stored in JSONB so new fields can appear without a database migration. Frequently used fields can later be promoted to dedicated indexed columns.</p></section></AppShell>}
