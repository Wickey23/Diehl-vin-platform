import { AppShell } from "../../components/app-shell";
export default function ImportPage(){return <AppShell><div className="page-title"><p className="eyebrow">DATA MANAGEMENT</p><h1>Import / Export</h1><p>Bring the current VIN master workbook and DTNA exports into the web database.</p></div><section className="grid-two"><article className="panel"><h2>Import VIN master</h2><p>Upload VIN_Master_Data.xlsx or CSV. Existing records are matched by VIN and identifiers rather than blindly duplicated.</p><input type="file" accept=".xlsx,.csv"/><div style={{marginTop:12}}><button className="primary">Import file</button></div></article><article className="panel"><h2>Export</h2><p>Generate an Excel-compatible export containing the current dynamic fields from the database.</p><button className="primary">Export vehicles</button></article></section><div className="notice">The import controls are UI-ready. Server-side XLSX parsing will be enabled after database environment variables are added.</div></AppShell>}
